# The miner is stdlib-only by design (asyncio + urllib — the OpenRouter judge
# needs no SDK). Cloud SDKs are imported lazily per provider
# (CLOUD_PROVIDER=aws|azure): boto3 for S3/DynamoDB, azure-* for
# Blob/Table Storage.
boto3==1.34.144
botocore==1.34.144
azure-identity==1.19.0
azure-storage-blob==12.24.0
azure-data-tables==12.6.0
